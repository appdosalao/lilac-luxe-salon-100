## 2024-11-20 - [ARIA Labels on Icon Buttons]
**Learning:** Icon-only buttons used in list components for actions like "Edit" and "Delete" frequently lack ARIA labels, making them inaccessible to screen readers.
**Action:** Always verify list components and proactively add `aria-label` attributes to icon-only action buttons.
