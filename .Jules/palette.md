
## 2024-05-18 - Missing ARIA labels on Icon Buttons
**Learning:** Icon-only buttons (like those with `size="icon"`) often lack `aria-label` and native `title` attributes in this codebase.
**Action:** Systematically add `aria-label` and `title` attributes in Portuguese to all icon-only buttons to improve screen-reader accessibility and native tooltip behavior.
