// Placeholder para funcionalidades que ainda precisam ser implementadas
export const useDatabase = () => {
  return {
    loading: false,
    agendamentos: [],
    clientes: [],
    servicos: [],
    cronogramas: [],
    createAgendamento: async () => false,
    updateAgendamento: async () => false,
    isHorarioDisponivel: () => true,
  };
};

export const db = {
  // Placeholder para funcionalidades que ainda precisam ser implementadas
};